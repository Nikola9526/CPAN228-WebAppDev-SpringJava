spring.application.name=Week7_JPAFoodApp_Security

restaurant.name=Good Food Place

page.size=5
#pagination


# h2 settings
#the url connection string is not changed everytime you run the project (its fixed)
spring.datasource.generate-unique-name=false

spring.datasource.name=dish
#if no more name default its testdb
spring.datasource.username=nikola
spring.datasource.password=

#name asks hibernate at to create the schema
spring.jpa.hibernate.ddl-auto=create-drop
# JPa auto makes schema # no more schema.sql file

# data.sql waits for the schema to be created before it runs
spring.jpa.defer-datasource-initialization=true

## Displays the sql queries being excuited
#spring.jpa.show-sql=true
##Formats the sql queries
#spring.jpa.properties.hibernate.format_sql=true
